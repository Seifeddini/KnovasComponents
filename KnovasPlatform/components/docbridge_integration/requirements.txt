# DocBridge-Knovas Integration Dependencies

# Database connectivity
pyodbc==5.1.0

# File system watching
watchdog==4.0.0

# HTTP requests
requests==2.32.0

# Configuration
pyyaml==6.0.2

# Web framework
flask==3.0.0
flask-cors==4.0.0
gunicorn==22.0.0

# Hashing and security
cryptography==42.0.0

# Per-user identity (Pflichtenheft B1/B2/B5): password verifiers, TOTP,
# the local account database, and schema validation for compiled
# RemoteController documents.
argon2-cffi==25.1.0
pyotp==2.10.0
psycopg[binary]==3.3.4
jsonschema==4.26.0

# Logging
python-json-logger==2.0.7

# Date/time handling
python-dateutil==2.8.2

# Retry logic
tenacity==8.2.3

# Document extraction (sentences, sections, tables for secured upload)
knovas-extract[pdf,docx,msg,markdown,html,sentences]>=0.3

# PDF-Textextraktion für die Cortex-Filter (Passagen-Matching)
pymupdf==1.28.0

# Environment variables
python-dotenv==1.0.0

# Testing
pytest==7.4.3
pytest-cov==4.1.0
pytest-mock==3.12.0

# Development
black==23.12.1
flake8==7.0.0
mypy==1.8.0
