# DocBridge-Knovas Integration Dependencies

# Database connectivity
pyodbc==5.1.0

# File system watching
watchdog==4.0.0

# HTTP requests
requests==2.32.0

# Configuration
pyyaml==6.0.2

# Web framework
flask==3.0.0
flask-cors==4.0.0
gunicorn==22.0.0

# Hashing and security
cryptography==42.0.0

# Per-user identity (Pflichtenheft B1/B2/B5): password verifiers, TOTP,
# the local account database, and schema validation for compiled
# RemoteController documents.
argon2-cffi==25.1.0
pyotp==2.10.0
psycopg[binary]==3.3.4
jsonschema==4.26.0

# Logging
python-json-logger==2.0.7

# Date/time handling
python-dateutil==2.8.2

# Retry logic
tenacity==8.2.3

# Document extraction (sentences, sections, tables for secured upload)
knovas-extract[pdf,docx,msg,markdown,html,sentences]>=0.2

# PDF-Textextraktion für die Cortex-Filter (Passagen-Matching)
pymupdf==1.28.0

# Environment variables
python-dotenv==1.0.0

# Testing
pytest==7.4.3
pytest-cov==4.1.0
pytest-mock==3.12.0

# Development
black==23.12.1
flake8==7.0.0
mypy==1.8.0

# Deutsche Wortstaemme fuer die Trefferhervorhebung: ohne sie ist
# "abgerechnet" etwas anderes als "Abrechnung", und der Satz mit der Antwort
# gilt als Stelle ohne jedes gesuchte Wort. Reines Python, BSD, kein Netz.
#
# Mindestens 3.0: die 2.2.0 mitgelieferten Stemmer sind aelter erzeugt und
# lassen die Verbendung stehen -- "abrechnet" bleibt "abrechnet", waehrend
# "Abrechnung" zu "abrechn" wird. Die beiden finden dann nicht zusammen, und
# genau der Satz, um den es geht, faellt wieder heraus.
snowballstemmer>=3.0,<4
